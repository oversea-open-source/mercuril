<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.messagecenter.portal.mapper.SMSTemplateMapper">
<select id="getById" resultType="SMSTemplateEntity">
    select * from cp_sms_template
    WHERE id = #{id}
</select>
</mapper>
